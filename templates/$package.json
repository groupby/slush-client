{
  "name": "<%= serviceNameSlug %>-client",
  "version": "0.0.0-development",
  "description": "<%= appDescription %>",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "repository": "https://github.com/groupby/<%= serviceNameSlug %>-client-javascript",
  "author": "<%= authorName %> (<%= authorEmail %>)",
  "scripts": {
    "start": "start-runner -p re-start/presets/typescript",
    "prepush": "yarn start prepush",
    "prepublish": "yarn start build"
  },
  "release": {
    "verifyConditions": "condition-circle"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^3.4.34",
    "@types/mocha": "^2.2.38",
    "chai": "^3.5.0",
    "condition-circle": "^1.5.0",
    "cz-conventional-changelog": "^1.2.0",
    "husky": "^0.13.0",
    "mocha": "^3.2.0",
    "re-start": "^1.8.1",
    "start-simple-cli": "^4.1.1",
    "tslint-config-groupby": "^1.0.0"
  },
  "dependencies": {
    "groupby-client-core": "^1.0.1"
  }
}
